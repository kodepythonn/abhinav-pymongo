- Setup Environment.

```
-- 1
python2 -m venv venv

-- 2 in windows
venv\Scripts\activate

-- 2 in linxu
source venv/bin/activate

-- 3 Add requirements.txt

-- 4 Add .gitignore file
Make Sure you have gitignore extension installed in vscode

-- 5 Add to github
echo "# abhinav-pymongo" >> README.md
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/kodepythonn/abhinav-pymongo.git
git push -u origin main


-- 6 Add Mongo Depdenncy
# Core dependencies
pymongo==4.5.0  # Library to interact with MongoDB

-- 7 Update Dependncy
pip install -r requirements.txt


```

# abhinav-pymongo
